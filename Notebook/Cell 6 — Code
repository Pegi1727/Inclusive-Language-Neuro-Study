metadata = read_edf_header(EDF_PATH)

sampling_rates = np.array(
    metadata["samples_per_record"],
    dtype=float
) / metadata["record_duration"]

total_duration = (
    metadata["number_of_records["record_duration"]

total_duration = (
    metadata["number_of_records metadata["version"])
print("Patient ID:", metadata["patient_id"])
print("Recording ID:", metadata["recording_id"])
print("Start date:", metadata["start_date"])
print("Start time:", metadata["start_time"])
print("Number of signals:", metadata["number_of_signals"])
print("Number of records:", metadata["number_of_records"])
print("Record duration:", metadata["record_duration"], "seconds")
print("Total duration:", total_duration, "seconds")
print("Channels:", metadata["labels"])
print("Sampling rates:", sampling_rates, "Hz")
